# 🔍 Инструкция по диагностике проблемы

## Шаг 1: Проверка сервера

1. Убедитесь, что запущен **тестовый сервер**:
```bash
npm run test
```

2. В терминале должна быть строка:
```
✅ Сервер запущен на http://localhost:3000
```

## Шаг 2: Проверка сохранения форм

1. Откройте Chrome DevTools (F12)
2. Перейдите на вкладку **Console**
3. Войдите как `admin` / `admin`
4. Заполните любую форму (например, первую строку первой таблицы)
5. Нажмите **"Сохранить"**

### Ожидаемый вывод в консоли:
```
💾 Начинаем сохранение форм: {...}
📋 Форма 1: hasData=true {...}
📤 Отправляем форму 1 на сервер...
✅ Ответ сервера для формы 1: {success: true, ...}
✅ Сохранено 1 форм
```

### Если видите ошибки:
- `❌ Ошибка сохранения формы` - проблема с подключением
- `hasData=false` для всех форм - данные не собираются

## Шаг 3: Проверка админки

1. Нажмите кнопку **"Админка"** в правом верхнем углу
2. Откройте Console (F12)

### Ожидаемый вывод:
```
📥 Загружено документов: {success: true, count: 1, documents: [...]}
📊 JSON форм: 1 [{...}]
🔄 Обновление фильтров, всего форм: 1
👤 Пользователей в фильтре: 1
🏢 Организаций в фильтре: 1
📋 Форма 1: найдено 1 записей
🔍 После фильтров: 1 записей
```

### Если видите:
- `📊 JSON форм: 0` - формы не сохраняются или сохраняются как DOCX
- `📋 Форма 1: найдено 0 записей` - проблема с фильтрацией

## Шаг 4: Проверка файла documents.json

1. Откройте файл `server/documents.json`
2. Должны увидеть примерно такую структуру:

```json
{
  "documents": [
    {
      "id": "...",
      "type": "json",
      "formNumber": "1",
      "formData": {
        "header": {...},
        "tables": {...}
      },
      "userId": "admin001",
      "username": "admin",
      "userFullName": "Администратор системы",
      "userOrganization": "..."
    }
  ]
}
```

### Если файл пустой или с другой структурой:
- Формы не сохраняются
- Нужно проверить логи сервера в терминале

## Шаг 5: Что отправить мне

Скопируйте и отправьте:

1. **Из консоли браузера** (после попытки сохранить форму):
   - Все сообщения начинающиеся с 💾, 📋, 📤, ✅, ❌

2. **Из консоли браузера** (в админке):
   - Все сообщения начинающиеся с 📥, 📊, 🔄, 👤, 🏢, 📋

3. **Содержимое файла** `server/documents.json` (первые 50 строк)

Это поможет понять, на каком этапе происходит сбой!
